#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
  int v,n,index;
  cin >> v;
  cin >> n;
  vector<int> ar(n);
  for(int i = 0; i < n; i++){
    cin >> ar[i];
    if(v == ar[i])
      index = i;
  }
  cout << index << endl;
  return 0;
}
